# build artifacts
build/
dist/

# toolchains, tarballs and cached static OpenSSL (see tools/build-release.sh)
deps/

# note: src/certs.h is generated by tools/mkcerts.sh but is committed so the
# project builds on any distro (mkcerts.sh needs an Arch-style CA bundle);
# regenerate it only when the embedded CA roots rotate.
